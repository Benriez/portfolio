---
/**
 * SectionHeader
 *
 * Editorial section header. The OpenDesign reference uses single-language
 * (HR) text for eyebrow and h2; we follow that exactly. The optional lede
 * is the only field that carries a real HR/Engineering mode split.
 */
import type { DualCopy } from "~/types/content";

interface Props {
  readonly id?: string;
  readonly eyebrow: DualCopy;
  readonly title: DualCopy;
  readonly lede?: DualCopy;
}

const { id, eyebrow, title, lede } = Astro.props as Props;
---

<header class="section-head" id={id}>
  <p class="section-kicker">{eyebrow.hr}</p>
  <h2>{title.hr}</h2>
  {lede && (
    <p class="section-lead">
      <span class="hr-only">{lede.hr}</span>
      <span class="engineering-only">{lede.engineering}</span>
    </p>
  )}
</header>

<style>
  .section-head {
    display: grid;
    grid-template-columns: minmax(0, 0.82fr) minmax(0, 1fr);
    gap: clamp(28px, 6vw, 80px);
    align-items: end;
    margin-bottom: 42px;
  }
  .section-head .section-kicker {
    grid-column: 1 / -1;
    margin-bottom: -18px;
  }
  .section-head h2 {
    font-size: var(--type-h1);
    line-height: 1.02;
    letter-spacing: -0.04em;
    font-weight: 600;
    max-width: 13ch;
    margin: 0;
  }
  .section-lead {
    margin: 0;
    color: var(--fg-soft);
    font-size: clamp(18px, 2vw, 24px);
    line-height: 1.35;
    letter-spacing: -0.02em;
  }
  @media (max-width: 900px) {
    .section-head {
      grid-template-columns: 1fr;
    }
  }
</style>
